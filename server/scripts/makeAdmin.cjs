// makeAdmin.cjs - run with Node to set admin role for a specific email\nrequire('dotenv').config();\nconst mongoose = require('mongoose');\nconst User = require('../models/user.js'); // path relative to this script\n\nconst ADMIN_EMAIL = 'aniketdev005@gmail.com';\n\n(async () => {\n  try {\n    // Connect using default options (no deprecated flags)\n    await mongoose.connect(process.env.MONGO_URI);\n    console.log('Connected to MongoDB');\n    const result = await User.updateOne({ email: ADMIN_EMAIL }, { role: 'admin' });\n    console.log(`Matched ${result.matchedCount} document(s), modified ${result.modifiedCount} document(s).`);\n    process.exit(0);\n  } catch (err) {\n    console.error('Error updating admin role:', err);\n    process.exit(1);\n  }\n})();\n
